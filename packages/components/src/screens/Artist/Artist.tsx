import React from 'react';
import { Text } from 'react-native';

const Artist = ({ id }: ArtistProps) => {
  return <Text>{'erjhfbejhrfge'}</Text>;
};

export default Artist;

interface ArtistProps {
  id: string;
}
