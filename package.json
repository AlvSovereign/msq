{
  "name": "myprojectname",
  "version": "0.0.1",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "env:dev": "node packages/shared/scripts/setEnvironment.js development",
    "android": "yarn run env:dev && yarn workspace mobile android",
    "compile": "tsc -b --incremental",
    "ios": "yarn run env:dev && yarn workspace mobile  ios",
    "studio": "yarn workspace mobile studio",
    "web": "yarn workspace web start",
    "xcode": "yarn workspace mobile xcode"
  },
  "dependencies": {
    "react-native": "*"
  },
  "devDependencies": {
    "typescript": "3.7.5"
  }
}
